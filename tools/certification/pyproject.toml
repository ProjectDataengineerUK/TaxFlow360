[project]
name = "taxflow-certification"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic==2.10.6", "PyYAML==6.0.2"]
[project.scripts]
taxflow-certify = "taxflow_certification.cli:main"
[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
